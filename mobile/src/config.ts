// Ships as the default so a real Play Store user never has to type in a
// server address — update this once the backend has a permanent home
// (see DEPLOY.md). Overridable in Settings for local development/testing.
export const DEFAULT_API_BASE_URL = 'http://192.168.0.134:5080';

// From your Google Cloud OAuth consent screen — see DEPLOY.md for how to
// create these. Required for the "Continue with Google" button to work.
export const GOOGLE_WEB_CLIENT_ID = '';
export const GOOGLE_ANDROID_CLIENT_ID = '';
